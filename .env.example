# Must match ripple-backend PORT (see ripple-backend/.env PORT=...)
VITE_API_URL=http://127.0.0.1:3007/api/v1
# Optional — derived from VITE_API_URL if omitted
# VITE_SOCKET_URL=http://127.0.0.1:3007
