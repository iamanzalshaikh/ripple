# P8.5 Remaining Work Tracker

**Last updated:** 2026-07-08  

**Overall progress:** ~**88% complete** (~12% remaining)

**Planner v2:** [P8.5-PLANNER-V2.md](./P8.5-PLANNER-V2.md) — **v2.0–v2.4 implemented** (v2.5 GPT context pending)

| Phase | Status | Weight | Done |
|-------|--------|--------|------|
| Phase 0 — Scaffold | ✅ Complete | 10% | 10% |
| Phase 1 — Desktop tools | ✅ Complete | 15% | 15% |
| Phase 2 — Safety + pipeline | ✅ Complete | 15% | 15% |
| Phase 3 — Shadow parity | ✅ Complete | 10% | 10% |
| Phase 4 — Delete routers | 🟡 In progress | 15% | ~11% |
| Phase 5 — Tool catalog | 🟡 Started | 20% | ~14% |
| Planner v2 (L2–L5) | ✅ v2.0–v2.4 | — | compound + atomic routing |
| Production metadata / §15 | 🔴 Open | 15% | ~2% |

**Executable tools:** 20 / 20–30 target  

(11 desktop + 6 filesystem + 2 system clipboard + 1 browser.linkedin.run)

**Tests:** `npm run test:p85` — **560 passed** (incl. `phase-p85-linkedin-tool.spec.ts`, `phase-p85-context-routing.spec.ts`)

**Env:** `RIPPLE_P85_PLANNER_V2=all` in `ripple-desktop/.env` (use `0` to disable; tests force `0` in vitest.setup)

---

## Phase 4 — Delete routers (~4% remaining)

- [x] Tool Executor default ON (`RIPPLE_P85_TOOL_EXECUTOR=0` to disable)
- [x] `legacyRouterGate.ts` — centralized kill switches
- [x] `desktop-fast` / `desktop-input-fast` shadow-only by default
- [x] `executionRequest.ts` — validated plan execution API
- [x] Removed duplicate shadow-only `logPlannerRouterMismatch` blocks (defer logged in `tryP85FastPath`)
- [ ] Delete dead `desktop-fast` / `desktop-input-fast` router **functions** when kill switches removed (kept behind `RIPPLE_P85_LEGACY_DESKTOP_FAST=1` until shadow metrics green)
- [x] Remove WhatsApp **early orchestrator blocks** → `browser.whatsapp.send` + L0 planner (`l0WhatsAppPlanner.ts`)
- [x] WhatsApp stabilization — contact parsing, pronouns, `Send X: Y`, unknown contact ask tier (`phase-p85-whatsapp-final.spec.ts`)
- [x] WhatsApp **referential** (`Send it to Noor` / file sends) → `l0WhatsAppPlanner` referential mode + orchestrator block removed
- [x] Remove YouTube **early orchestrator blocks** → `l0YouTubePlanner` + `browser.youtube.run`
- [x] Remove Gmail from bypass → `l0GmailPlanner` + `browser.gmail.compose`
- [x] Remove LinkedIn **early + late orchestrator blocks** → `l0LinkedInPlanner` + `browser.linkedin.run`
- [x] Shadow parity skips adapter tools (`browser.whatsapp.*`, `youtube`, `gmail`, `linkedin`) — reduces noisy `desktop-fast` mismatch logs
- [ ] Remove duplicate `planDesktopCommand` paths (`buildDesktopCommandResultAsync` unused; orchestrator is sole runtime caller when `RIPPLE_P85_LEGACY_PLAN=1`)
- [ ] Single orchestrator entry (only safety pre-gates before Universal Planner — Instagram/Notion locals still after P85 defer)
- [ ] Real-usage shadow metrics green before final deletion

**Still in orchestrator (intentional rollback / not yet migrated):**

- `desktop-fast` / `desktop-input-fast` — `RIPPLE_P85_LEGACY_DESKTOP_FAST=1`
- `planDesktopCommand` — `RIPPLE_P85_LEGACY_PLAN=1`
- `agent-compound` — `RIPPLE_P85_LEGACY_AGENT_COMPOUND=1`
- `notion-local`, `instagram-local` — no L0 tool planners yet

---

## Phase 5 — Tool catalog (~6% remaining)

### Done

- [x] Wave 1: 11 `desktop.*` tools with `execute()`
- [x] Wave 2: 6 `filesystem.*` tools (incl. open)
- [x] `system.clipboard.read` / `system.clipboard.write` with `execute()`
- [x] L0 → `filesystem.*` plan steps (`l0FileOpPlanner.ts`)
- [x] NLU file mutators → `filesystem.*` via `desktopPayloadToFilesystem.ts`
- [x] Manifest v1.2.0
- [x] `browser.whatsapp.send` — registered `execute()` in `browserTools.ts`, L0 planner wired
- [x] `browser.youtube.run` — L0 planner wired
- [x] `browser.gmail.compose` — registry execute + L0 planner
- [x] `browser.linkedin.run` — registry execute + L0 planner

### Still needed

- [ ] `filesystem.list_directory` / search tools
- [ ] L0 utterance fixtures for all file-op variants in shadow parity

---

## Pipeline / production metadata (~13% remaining)

- [x] `dependsOnTools` enforcement in tool executor
- [ ] Plan validator runs twice (early L0 + final) — only final wired today
- [ ] Batch cap in planValidator after entityResolver expansion
- [ ] Failure taxonomy (`FailureKind`) typed in recovery
- [ ] Observation confidence on filesystem steps
- [ ] Full entity resolver (graph, retriever, contacts) — app phrase + memory only today
- [ ] Tool health rolling window
- [ ] ExecutionPlan cache
- [ ] Stage latency metrics

---

## Success criteria gaps (§15)

- [ ] Multi-step E2E: "open notepad type hello" through executor (not payload bridge)
- [ ] Router mismatch rate 0 in dev over N days
- [ ] +30 executor integration tests (currently ~560 in test:p85)
- [ ] Manifest auto-gen with per-tool examples
- [ ] `readyForDeprecation` sustained in real usage (not just fixtures)

---

## Explicitly NOT P8.5

- Event bus, plugins, Execution Graph, Tool Sessions implementation
- OCR / vision, full browser suite, open-ended replan (P9)
- Instagram / Notion L0 migration (deferred after core desktop + filesystem stable)

---

## Recommended next steps (priority order)

1. **Phase 4 finish** — Instagram/Notion L0 or remove locals; delete `desktop-fast` functions after shadow metrics green
2. **`filesystem.list_directory`** — read-only directory listing tool
3. **Compound L0** — open notepad + type through executor (not `_desktopPayload`)
4. **Shadow fixtures** for NLU file-op utterances
5. **Plan validator** early pass before GPT fallback
