/** Not in Phase 3.5 B4 scope — search + open only. */
export async function commentOnYouTubeVideo(_message: string): Promise<string> {
  throw new Error(
    "YouTube comments are not supported yet — say e.g. search React tutorial on YouTube",
  );
}
